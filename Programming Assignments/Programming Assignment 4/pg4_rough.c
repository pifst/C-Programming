#include <stdio.h>

#define MAXIMUMSTUDENTS 50


struct studentRecord Table[MAXIMUMSTUDENTS];

int main(void)
{
    fillArrays(struct studentRecord)
    setGrades(struct studentRecord)
        - Set letter grade, one student (structure) at a time.
        - Iterate over array of [struct studentRecord]
    averageGrade(struct studentRecord)
        - return average to main
    print classAverage
    bestStudent(struct studentRecord)
        - return specific studentRecord
    print Best ID, Numeric Score, Lettergrade
    print (classGrades[A,B,C,D,F])
    print studentCountAboveAverage

}
